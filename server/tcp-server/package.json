{
  "name": "mototakip-tcp-server",
  "version": "1.0.0",
  "description": "Handles TCP messages and send to process queues them",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon --watch src/ --inspect=0.0.0.0 src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yunus Oksuz",
  "license": "MIT",
  "dependencies": {
    "mototakip-common": "file:../../common",
    "mototakip-protocols": "file:../../protocols"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
